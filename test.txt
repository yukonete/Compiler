type Vec2 = struct {
	x: f32;
	y: f32;
};

proc main(argv: *int, argc: int) -> int {
	a: Vec2;
	const b: Vec2 = 0;

	if a > b {
		a = a + 4;
	} else {
		b = -b;
	}

	i: int = 0;
	while i < 10 {
		a = a - 1;
	}

	return 0;
}